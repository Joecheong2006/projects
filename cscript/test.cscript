val=-((1+2)*(4-1)+(4+2)*(4-1)-(1+2)*(4-1)-(4+2)*(4-1)-1-1-1)*1.5
a=0b11
str='hi'
c=str

c='a'+1
c='b'-1

val=1.11*10.0+a+c

a=69
c+=1
c/=2
c=c+1

fn pow(base, ex)
    base *= ex
    return base
end

a = 3
a = pow(pow(pow(2, 2), pow(1, 2)), pow(pow(3, 1), a))
