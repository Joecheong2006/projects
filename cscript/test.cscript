const var number = 3 * (2 + 9) // arithmetic operation

// string support
var str = "hello, world"

// base 16 support
const int base16 = 0x123456789
// base 2 support
const int base2 = 0b100101001

float b = 3.0

var array = [1, 2, 3, 4]

// (if) statement (then) body (end)
if true then
    body
end

def pow(float base, int e)->float
    return base**e
