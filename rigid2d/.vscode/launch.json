{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(gdb) Launch",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/bin/balls_collision_test.exe",
            "stopAtEntry": true,
            "cwd": "${workspaceFolder}"
        }
    ]
}
